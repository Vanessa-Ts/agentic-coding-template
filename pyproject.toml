[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "agentic-coding-template"
version = "0.1.1"
requires-python = ">=3.11,<3.14"
authors = [{name = "VanessaTs"}]
dependencies = [
    "fastapi~=0.136.0",
    "openai~=2.38.0",
    "uvicorn[standard]~=0.48.0",
    "pydantic~=2.13.0",
    "pydantic-settings~=2.14.0",
    "jinja2~=3.1.0",
    "python-dotenv~=1.2.0",
]

[dependency-groups]
dev = [
    { include-group = "test" },
    "black~=26.5.0",
    "isort~=8.0.0",
    "ruff~=0.15.0",
    "mypy~=2.1.0",
    "pyyaml>=6.0.3",
    "python-frontmatter>=1.3.0",
]
test = [
    "pytest~=8.3.0",
    "pytest-asyncio~=1.2.0",
    "pytest-cov~=7.1.0",
    "httpx~=0.28.0",
    "httpx2>=2.2.0",
]


[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests", "evals"]
pythonpath = ["src"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
addopts = [
    "-v",
    "-m", "not behavioral",
    "--cov=app",
    "--cov-report=term-missing",
    "--cov-report=xml:coverage.xml",
    "--junitxml=report.xml"
]
markers = ["behavioral: LLM-graded eval; run locally with -m behavioral"]

[tool.coverage.run]
source = ["app"]
omit = ["tests/*"]

[tool.black]
line-length = 88
target-version = ["py311"]

[tool.isort]
profile = "black"
line_length = 88

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "S"]
ignore = ["E402"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]
"evals/**" = ["S101", "S603", "S607", "E501"]
".claude/hooks/**" = ["S101", "S603", "S607"]


[tool.mypy]
python_version = "3.11"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_configs = true
warn_unused_ignores = true
disallow_incomplete_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_no_return = true
ignore_missing_imports = false
pretty = true

